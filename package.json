{
  "name": "codelah",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "bun run scripts/copy-pyodide.mjs",
    "dev": "vite",
    "prebuild": "bun run scripts/copy-pyodide.mjs",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b",
    "test:lesson": "node scripts/verify-calculator.mjs",
    "test:e2e": "CI=1 playwright test"
  },
  "dependencies": {
    "blockly": "latest",
    "pyodide": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@vitejs/plugin-react": "latest",
    "typescript": "latest",
    "vite": "latest"
  }
}
